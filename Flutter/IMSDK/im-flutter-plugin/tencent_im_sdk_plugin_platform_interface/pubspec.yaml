name: tencent_im_sdk_plugin_platform_interface
description: tencent_im_sdk_plugin插件的通用平台接口
version: 0.1.8
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
