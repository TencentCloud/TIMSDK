const HtmlWebpackPlugin = require('html-webpack-plugin');


module.exports = {
    mode: 'production',
    entry: './index.ts',
    output: {
        path:  __dirname + '/dist', // 打包后的输出目录
        filename: 'tls.min.js',
        libraryTarget:'umd',
        library: 'im-live-sells', // 库名称
        libraryExport: 'default', 
    },
    devServer: {
        contentBase: './dist',
        hot: true
    },
    plugins: [new HtmlWebpackPlugin()],
    module: {
        rules: [{
            test: /\.ts?$/,
            use: 'ts-loader',
            exclude: /node_modules/
        }]
    },
    resolve: {
        extensions: ['.ts', '.js']
    }
}