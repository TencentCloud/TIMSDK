const _getVarsByKey = (arr: Array<any>, key: string) => {
    let res
    for (let i = 0; i < arr.length; i++) {
        if (arr[i].key === key) {
            res = arr[i].value;
            break;
        }
    }
    return res
}

export {
    _getVarsByKey
}